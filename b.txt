From our side, the goal is to enable invoice document ingestion for our application workflow.
At a high level, the requirement is to allow users to upload invoice documents — typically PDFs or images — and then call an AI extraction capability that can return the structured invoice fields in JSON format.

Our application layer is built on the Microsoft stack, so we’ll likely expose an internal ASP.NET Web API endpoint that will call the AI extraction service provided by this platform.

What we’re hoping to understand today is how this capability is structured on your side — the APIs available, request/response patterns, and how we as developers can start contributing or integrating with the service.