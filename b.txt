{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",

  "required": [
    "documentKey",
    "documentType",
    "extractedAtUtc",
    "provider",
    "modelVersion",
    "overallConfidence",
    "invoice",
    "vendorMatchType",
    "vendorCandidates"
  ],

  "properties": {
    "documentKey": {
      "type": "string",
      "format": "uuid"
    },

    "documentType": {
      "type": "string"
    },

    "extractedAtUtc": {
      "type": "string",
      "format": "date-time"
    },

    "provider": {
      "type": "string"
    },

    "modelVersion": {
      "type": "string"
    },

    "overallConfidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },

    "invoice": {
      "type": "object",

      "properties": {
        "vendorText": {
          "type": ["string", "null"]
        },
        "invoiceDate": {
          "type": ["string", "null"],
          "format": "date"
        },
        "dueDate": {
          "type": ["string", "null"],
          "format": "date"
        },
        "checkDate": {
          "type": ["string", "null"],
          "format": "date"
        },
        "totalAmount": {
          "type": ["number", "null"]
        },
        "summaryDescription": {
          "type": ["string", "null"]
        }
      }
    },

    "vendorMatchType": {
      "type": "string"
    },

    "vendorCandidates": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "vendorId",
          "vendorKey",
          "vendorName"
        ],
        "properties": {
          "vendorId": {
            "type": "number"
          },
          "vendorKey": {
            "type": "number"
          },
          "vendorName": {
            "type": "string"
          }
        }
      }
    }
  }
}